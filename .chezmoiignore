# Plugin dirs managed by their own plugin managers — not chezmoi's job
.config/tmux/plugins/**
.config/nvim/lazy-lock.json
# chezmoi executes files with run_ prefix; overseer templates use that naming convention
.config/nvim/lua/overseer/template/user/run_*

# Raycast manages its own extensions — not chezmoi's job
.config/raycast/**

# fcitx5 is Linux-only
{{ if ne .chezmoi.os "linux" }}
.config/fcitx5/**
{{ end }}
